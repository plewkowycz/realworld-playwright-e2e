# Base URL for the Angular app (Docker Compose exposes 4200)
# - App via Docker Compose (recommended here):
#     APP_BASE_URL=http://host.docker.internal:4200
# - App running directly on your host (no Docker):
#     APP_BASE_URL=http://localhost:4200
# For Backend use APP_API_URL 

# Primary test user (used by current tests).
# If E2E_EMAIL is omitted, tests default it to "${E2E_USERNAME}@example.com".
# E2E_USERNAME=
# E2E_PASSWORD=
# E2E_EMAIL=

# Optional additional users for multi-user scenarios.

# Secondary test user (used by current tests).
# E2E_SECONDARY_USERNAME=
# E2E_SECONDARY_PASSWORD=
# E2E_SECONDARY_EMAIL=