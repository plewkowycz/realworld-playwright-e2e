# E2E Test Configuration
# Copy this file to .env and adjust if needed: cp .env.example .env

# Base URL for the Angular frontend
# Use localhost for both local and dockerized test runs
# (The test container uses network_mode: host to access localhost)
APP_BASE_URL=http://localhost:4200

# Base URL for the Django REST API backend
APP_API_URL=http://localhost:8000
